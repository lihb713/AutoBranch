## 1. 基础数据模型与类型契约

- [x] 1.1 定义基础节点数据模型（Action / Condition / Sequence / Selector / Repeat / Finish）与 `BranchSpec`，字段对齐 `docs/contract.md` §5.7.2 与 M2 spec §5.2；验证：单元测试断言节点类型与字段（如 Repeat 的 mode/until/max）符合契约
- [x] 1.2 定义解析输出契约 `ParseResult`（tree / blocks / checks）、块声明 `BlockDecl`（输入/输出/配置参数覆盖）、校验报告 `CheckReport`、文档源 `DocumentSource` 与引用解析器 `RefResolver` 抽象接口；验证：类型注解可通过静态检查（`pytest` 导入断言 + 无 `mypy` 报错）
- [x] 1.3 定义 yaml 文本与 dict 的统一规范化入口（yaml 文本先转 dict 再走同一解析路径）与解析错误类型（非 yaml/dict、非法结构）；验证：对 yaml 文本、dict、非法输入各写单测断言行为一致且错误类型可识别

## 2. 结构解析器（中间表示）

- [x] 2.1 实现文档结构解析：识别操作块定义（命名块集合）与根流程，产出保留复合节点与 `ref:` 的中间表示；验证：纯函数表驱动测试，对多组文档断言中间表示结构
- [x] 2.2 实现块声明表提取：从每个命名块（含根块）提取输入/输出声明；验证：单测断言 `blocks` 的输入/输出列表与文档声明一致
- [x] 2.3 实现配置参数覆盖识别：在块 schema 下识别 timeout/retry 等配置参数覆盖并标注生效范围（当前块及子树），未声明不要求用户书写；验证：单测断言覆盖提取正确、未声明时输出不含该参数覆盖

## 3. 复合节点展开

- [x] 3.1 实现展开规则映射（Step=B+Condition 即 Sequence、Branch=Action+Selector、LoopUntil/Retry=Repeat 两模式、IfThenElse=Selector），规则集中于单一映射表；验证：表驱动测试，每个复合节点多组样例断言展开后的基础节点结构与 §4.3 精确语义一致
- [x] 3.2 增加展开结果全量断言：展开后的行为树仅含基础节点（Action / Condition / Sequence / Selector / Repeat / Finish）；验证：遍历断言无复合节点残留（含嵌套复合节点场景）
- [x] 3.3 处理深层嵌套（如 Retry 内嵌 Step、IfThenElse 内嵌 Branch）与递归上限防护；验证：深嵌套样例展开正确，超过上限时按校验失败处理

## 4. 块引用解析与 schema 绑定

- [x] 4.1 实现 `RefResolver` 抽象与解析逻辑：`this/块名`（当前文档内）、`文档名/块名`（跨文档某块）、`文档名/文档名`（跨文档整树）；验证：多文档 fixture 测试断言各引用形式解析结果正确
- [x] 4.2 引用处建立独立 schema 命名空间（调用栈帧语义，遵循 §5.7.4）并记录参数绑定关系（`写入` 注入被引用块输入）；验证：单测断言命名空间层级与绑定关系正确（对齐 §5.7.6 完整示例）
- [x] 4.3 实现循环引用检测（A↔B 相互引用）并判定校验失败；验证：环 fixture 测试断言报错且报告指明环路

## 5. 清晰度校验（§4.4 八类）

- [x] 5.1 实现结构合法性与展开后合法性校验（节点类型正确、嵌套有效）；验证：反例矩阵测试（非法节点类型、非法嵌套）断言失败且错误可定位
- [x] 5.2 实现块引用存在性与循环上界校验（`max` 缺失判定失败）；验证：反例矩阵测试（引用不存在、循环无上界）断言失败
- [x] 5.3 实现变量契约校验（只读写自己的 schema 与直接子块，越祖先/兄弟/孙子作用域判定失败）；验证：反例矩阵测试（越作用域引用）断言失败并指明位置
- [x] 5.4 实现可定位校验（Action 有 CSS 或有 LLM 可映射的自然语言）与验证条件校验（Step/Branch 缺 expect/判断判定失败）；验证：反例矩阵测试（不可定位、缺验证条件）断言失败
- [x] 5.5 实现条件谓词结构可校验（谓词指向可解析对象与可比较目标）；验证：反例矩阵测试（不可解析谓词）断言失败
- [x] 5.6 实现 `CheckReport` 汇总：通过/失败结论 + 逐条可读错误清单（含文档位置与违反规则）；验证：失败用例断言报告结构、结论与错误清单内容可读

## 6. 集成与验收

- [x] 6.1 集成测试：完整多文档 fixture（含复合节点、跨文档引用、配置覆盖、变量绑定）→ 断言输出纯基础节点行为树 + 块声明表 + 校验报告；验证：在 `webops` conda 环境运行 `pytest -m integration` 通过
- [x] 6.2 确定性测试：同一输入文档解析两次断言结构完全一致；验证：单测通过
- [x] 6.3 包注册与全量测试：`pip install -e .` 注册模块，运行 `pytest` 全绿且不依赖 LLM/浏览器；验证：`webops` 环境内 `pytest` 全部通过
- [x] 6.4 文档同步：按实现更新 `docs/contract.md`（§4/§4.3/§4.4、§5.7.3/§5.7.4/§5.7.5）与 `docs/specs/M2-behavior-tree-parser.md`，保持说明书与代码一致；验证：核对文档描述与实现行为一致