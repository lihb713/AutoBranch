## 1. 插件框架（M3）

- [x] 1.1 定义 `FunctionDef`（名字/说明/参数/多返回值/变量目标参数/实现）与插件类接口（`name`/`description`/`function_defs()`/`init(runtime)`/`call()`），实现 `@engine_function` 显式注册；单测验证仅标注函数入注册表。
- [x] 1.2 实现注册表（known/loaded/functions）与两条加载路径：启动扫描 `plugins/` 预置包（`import`，排除 `common`）+ 从 DB 加载自定义插件（`compile + exec` 注入注册器）；单测覆盖两路径。
- [x] 1.3 实现懒装配与统一分发：按函数名定位插件 → 未装配则 `init` → 调用 → 产出型工具由引擎落笔写变量；单测覆盖未加载函数被直接调用返回错误（不中断）。
- [x] 1.4 实现框架工具 `use_capability(name)` 与能力概览生成；单测验证选能力后其函数追加进工具集、复用工具调用机制。
- [x] 1.5 实现统一 reporting 接口（`FunctionResult.report` 由引擎落笔、按来源分组）；单测验证插件返回值中的报告附加信息被写入。

## 2. 浏览器插件（M7）

- [x] 2.1 建立 `plugins/browser` 包与 `plugins/common` 共享库，迁移浏览器驱动与语义图生成作为插件内部实现；验证 `plugins/` 可被加载器扫描（排除 `common`）。
- [x] 2.2 把浏览器函数（`open`/`activate`/`click`/`type`/`extract`/`semantic_graph` 等）迁移为插件函数（只返回值、不写变量）；单测覆盖函数注册与返回值。
- [x] 2.3 页面对象泛型化（`object` 类型）、当前活动页由插件维护、`semantic_graph` 强制截图并写入报告；集成测试（真实浏览器）验证打开/操作/看页面截图链路。

## 3. 行为树解析器（M1）

- [x] 3.1 实现 FunctionCall 节点解析（`function`/`args`/`returns`）与清晰度校验（函数存在、args 与签名对齐）；单测覆盖合法解析与两类失败。
- [x] 3.2 类型系统新增泛型对象类型 token（`object`），`inputs`/`outputs`/`returns` 支持声明；单测验证解析通过。

## 4. 叶子 agent（M4）与编排器（M5）

- [x] 4.1 叶子执行改为两级能力选择 + 懒装配触发 + 一次执行多轮多能力；移除强制语义图预取，改为提示词强调；单测验证纯计算叶子零浏览器开销。
- [x] 4.2 变量写入落笔：叶子执行器拆产出型工具的目标参数、调用函数、写当前帧变量；单测验证目标未声明时拒绝。
- [x] 4.3 编排器支持 FunctionCall 节点确定性执行 + 经插件框架分发/懒装配 + 运行结束释放重资源；单测覆盖直接调用、返回值回收、异常 = FAILURE。
- [x] 4.4 报告改为「引擎基础字段 + 插件附加」；单测验证非浏览器操作不产生截图、插件附加信息按来源展示。

## 5. 其余预置插件（M7）

- [x] 5.1 计算插件（数值运算/排序/比较，纯函数）；单测验证确定性。
- [x] 5.2 SSH 插件（创建/关闭会话、远程执行，会话对象为泛型对象）；单测（mock）覆盖会话创建与命令执行。
- [x] 5.3 文件插件（读/写/对比/路径）；单测覆盖读写与对比。

## 6. 插件管理（M8 后端 / M9 前端）

- [x] 6.1 建立 `plugin` 表（name/kind/description/functions/source/timestamps）+ 启动扫描初始化 builtin 记录（只读）；单测验证初始化与只读拒绝。
- [x] 6.2 实现插件 API（列出/读源码/新增/更新/删除/校验）与关联查询（扫描行为树 FunctionCall 引用）；单测覆盖校验约束（仅标准库等）与删除后置空。
- [x] 6.3 前端插件管理页（列表标记预置/自定义 + CodeMirror 6 编辑器 + 保存校验详尽提示 + 删除关联弹窗）；组件测试覆盖列表与校验提示。
- [x] 6.4 端到端测试（Playwright）：前端编辑/新增自定义插件并保存 → 生效，覆盖"编辑插件 → 保存 → 行为树可调用"链路。

## 7. 文档同步

- [x] 7.1 按新模块（M0–M9）重编排 `docs/specs/`，更新 `docs/contract.md`（插件框架、FunctionCall、泛型对象、报告、模块结构）。
- [x] 7.2 移除/合并旧模块 spec（M5 引擎函数、浏览器驱动、语义图），确保文档与代码一致。
