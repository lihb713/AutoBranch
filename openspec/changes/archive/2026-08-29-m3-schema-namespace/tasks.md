## 1. 基础数据模型与路径工具

- [x] 1.1 定义 Value 类型与 SchemaFrame 数据结构（帧路径、parent 指针、内部平铺存储），验证为纯内存结构、无任何 I/O
- [x] 1.2 实现路径解析工具（按 `/` 分层、提取首段目标帧），验证单测覆盖合法路径、空路径、含 `/` 变量名被拒绝
- [x] 1.3 定义 SUPPORTED_TYPES 类型登记表，验证可枚举金额/订单号/URL/日期/文本/页面引用/布尔等类型

## 2. 帧管理与严格作用域

- [x] 2.1 实现 SchemaSpace.enter_block（每次块引用创建独立帧、挂接 parent），验证帧隔离测试：两次引用同名变量互不冲突
- [x] 2.2 实现 write/read 作用域校验（只允许自身帧 + 直接子帧；祖先/兄弟/孙子被拒绝并报越权校验错误），验证作用域规则矩阵测试全组合通过
- [x] 2.3 实现传参逐层传递与取子块返回值（写/读直接子帧路径），验证 T→登录→输入框 传参链与「读直接子帧取 result」用例通过

## 3. 配置参数继承

- [x] 3.1 实现 resolve_config 向上查找（自身 → 最近祖先 → 根级全局默认），验证继承链三级测试：块覆盖优先、向上找最近祖先、全局兜底
- [x] 3.2 实现全局默认注入（SchemaSpace 初始化时注入根级帧），验证注入后各子帧继承解析正确
- [x] 3.3 验证业务变量不向上查找：读自身未定义业务变量返回空值，不取祖先同名值

## 4. 类型契约与断言失败

- [x] 4.1 实现写入时类型声明与即时校验，验证各类型合法值写入成功
- [x] 4.2 实现提取/读取时强校验，验证非法类型值在写入与提取两处均触发断言失败并终止
- [x] 4.3 定义统一断言失败异常类型，验证异常语义可被 M7 捕获并沿树传播（§5.7.7）

## 5. 页面变量机制

- [x] 5.1 实现 PageRef 类型值的存储与读写，验证 open 返回值写入页面引用变量成功
- [x] 5.2 验证一个帧可持有多个页面变量且互不覆盖
- [x] 5.3 实现页面变量按普通参数传参（父块写子帧），验证子块读取到同一页面引用
- [x] 5.4 提供当前页面变量解析接口（绑定目标页），验证操作绑定指向当前页面变量所指页

## 6. 集成与文档

- [x] 6.1 运行 `pytest` 跑通 M3 全部单元测试（作用域矩阵/继承链/类型校验/帧隔离/页面变量），验证全绿且无外部依赖
- [x] 6.2 同步更新 `docs/specs/M3-schema-namespace.md` 与 `docs/contract.md` 相关章节，验证文档与实现行为一致